class Library:
    def __init__(self, ID, n_books, sign_up_days, books_per_day, books):
        self.ID = ID
        self.books = n_books
        self.sign_up_days = sign_up_days
        self.books_per_day = books_per_day
        self.books = books

